const powerUp = "mushroom";
let effect = powerUp === "mushroom" ? "Mario becomes Super!":
            powerUp === "flower" ? "Mario can shoot fireballs!":
            powerUp === "star" ? "Mario is invincible!": "Mario is normal!"
console.log(effect);